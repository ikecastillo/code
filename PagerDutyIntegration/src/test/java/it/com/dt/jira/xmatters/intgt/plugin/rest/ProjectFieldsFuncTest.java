package it.com.dt.jira.xmatters.intgt.plugin.rest;

import org.junit.Test;
import org.junit.After;
import org.junit.Before;
import org.mockito.Mockito;
import static org.junit.Assert.*;
import static org.mockito.Mockito.*;

import org.apache.wink.client.Resource;
import org.apache.wink.client.RestClient;

public class ProjectFieldsFuncTest {

    @Before
    public void setup() {

    }

    @After
    public void tearDown() {

    }

    @Test
    public void messageIsValid() {

        /*String baseUrl = System.getProperty("baseurl");
        String resourceUrl = baseUrl + "/rest/projectfields/1.0/message";

        RestClient client = new RestClient();
        Resource resource = client.resource(resourceUrl);

        ProjectFieldsModel message = resource.get(ProjectFieldsModel.class);*/

        /*assertEquals("wrong message","Hello World",message.getMessage());*/

        //TODO see if we can add some tests later on
    }
}
