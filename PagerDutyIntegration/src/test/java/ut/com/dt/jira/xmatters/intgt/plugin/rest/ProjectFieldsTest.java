package ut.com.dt.jira.xmatters.intgt.plugin.rest;

import org.junit.Test;
import org.junit.After;
import org.junit.Before;
import org.mockito.Mockito;
import static org.junit.Assert.*;
import static org.mockito.Mockito.*;

import javax.ws.rs.core.Response;
import javax.ws.rs.core.GenericEntity;

public class ProjectFieldsTest {

    @Before
    public void setup() {

    }

    @After
    public void tearDown() {

    }

    @Test
    public void messageIsValid() {
        /*ProjectFields resource = new ProjectFields();
        String projectKey = "ITIM";

        Response response = resource.getMessageFromPath(projectKey);
        final ProjectFieldsModel message = (ProjectFieldsModel) response.getEntity();*/

        //assertEquals("wrong message","ITIM",message.getMessage());
        //TODO see if we can add some tests later on
    }
}
